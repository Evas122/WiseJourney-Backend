Based on the last assistant message:
{{$last_assistant_messages}}
Extract and complete the following JSON schema in English based on the preferences provided in the last assistant messages.
Ensure all fields are filled appropriately using the details given. If some details are missing,
fill them with reasonable defaults or placeholders. The schema must strictly match the structure below:

{
  "DestinationType": "string",
  "Budget": 0.0,
  "DurationInDays": 0,
  "TravelStyles": [
    "string"
  ],
  "ClimatPreference": "string",
  "AccommodationType": "string",
  "TransportTypes": [
    "string"
  ],
  "Cuisines": [
    "string"
  ],
  "MaxDistanceKm": 0,
  "Activities": [
    "string"
  ],
  "SpecificLocations": [
    "string"
  ],
  "DeparturePoint": "string"
}

Ensure all string fields are in English, and numeric fields use appropriate values or 0 as a placeholder. Respond only with the JSON schema, nothing else.