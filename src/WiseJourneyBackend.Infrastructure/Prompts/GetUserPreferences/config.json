{
  "schema": 1,
  "type": "completion",
  "description": "Fill up json schema based on chat history.",
  "execution_settings": {
    "default": {
      "temperature": 0.8
    }
  },
  "input_variables": [
    {
      "name": "last_assistant_messages",
      "description": "last assistant messages with information to make user preferences.",
      "required": true
    }
  ]
}